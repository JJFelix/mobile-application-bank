/**
 * @format
 */

import {AppRegistry} from 'react-native';
import App from './scripts/App';
import {name as appName} from '../Bank-mobile-app/app.json';

AppRegistry.registerComponent(appName, () => App);
